import 'package:flutter/material.dart';

class MyAlbumsPageBody extends StatelessWidget {
  const MyAlbumsPageBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
